/* 
project.c - Compile this! This is the fully compileable and linkable barebones project file.
*/
#include "kernel.c" //Include the kernel

char userInit(void){ //On init 
	return 0;
}

char userLoop(void){ //On loop
	return 0;
}

void userPanic(void){ //On kernel panic
	return;
}
